<div class="parallax-container valign-wrapper">
    {{#if profile.coverUrl}}
    <div class="parallax col s12"><img class="responsive-img" src="{{profile.coverUrl}}"></div>
    {{else}}
    <div class="parallax col s12"><img class="responsive-img" src="/default/front-default.png"></div>
    {{/if}}

    {{#if profile.coverUrl}}
    <img class="profile-avatar circle" src="{{profile.avatarUrl}}">
    <h2 class="profile-name white-text">{{profile.name}}</h2>
    {{else}}
    <img class="profile-avatar circle" src="/default/avatar-default.png">
    <h2 class="profile-name white-text">{{profile.name}}</h2>
    {{/if}}
</div>
<div class="container">
    <div class="row">
        <div class="col s12 m6">
            <ul id="my-rooms" class="collection">
                <!--
                <li class="collection-item avatar">
                    <a href="/room/id/{{_id}}"><img src="/default/front-default.png" alt="" class="circle"></a>
                    <span class="title"><strong>{{name}}</strong></span><br/>
                    <span>{{lastMessage.sender}}: </span>
                    <span>{{lastMessage.message}}</span><br/>
                    <span class="right"small>{{lastMessage.date}}</small></span><br/>
                </li>
                -->
            </ul>
        </div>
        <div class="col s12 m6">
            <ul id="favorite-rooms" class="collection">
                <!--
                <li class="collection-item avatar">
                    <a href="/room/id/{{_id}}"><img src="{{coverUrl}}" alt="" class="circle"></a>
                    <span class="title"><strong>{{name}}</strong></span><br/>
                    <span>{{lastMessage.sender}}: </span>
                    <span>{{lastMessage.message}}</span><br/>
                    <span class="right"small>{{lastMessage.date}}</small></span><br/>
                    <a href="#!" class="secondary-content"><i class="material-icons">grade</i></a>
                </li>
                -->
            </ul>
        </div>
    </div>
</div>
<div class="fixed-action-btn">
    <a href="/profile/id/{{profile.id}}/config" class="btn-floating btn-large"><i class="large material-icons">mode_edit</i></a>
</div>

<script type="application/javascript" src="/socket.io/socket.io.js"></script>
<script type="application/javascript" src="/js/profileSocketClient.js"></script>